import type { ApiClient } from "../api/client.js";import type { Config } from "../config.js";import type { SessionStore } from "../session/store.js";import { agenticPrompt } from "../prompts/agentic.js";import { toolError,toolText } from "../utils/responses.js";import { continueTaskSchema } from "../utils/validation.js";
export {continueTaskSchema};export const continueTask=(client:ApiClient,config:Config,sessions:SessionStore)=>async(input:unknown)=>{try{const a=continueTaskSchema.parse(input),previous=await sessions.get(a.sessionId),result=await client.complete({model:config.textModel,messages:[{role:"system",content:agenticPrompt(a.maxSteps)},{role:"user",content:`Original objective:\n${previous.objective}\n\nPrior summary:\n${previous.summary}\n\nUnresolved issues:\n${previous.unresolvedIssues.join("\n")||"None recorded"}\n\nNew instruction:\n${a.instruction}${a.additionalContext?`\n\n<untrusted_context>\n${a.additionalContext}\n</untrusted_context>`:""}`} ]}),updated=await sessions.update(a.sessionId,{summary:result.text.slice(0,5000),completed:false});return toolText(result,{sessionId:updated.id,sessionUpdatedAt:updated.updatedAt,actualToolExecutionOccurred:false});}catch(error){return toolError(error);}};
