# Workspaces

`glm_5_inspect_workspace` currently reports:

- the inspected working directory
- package manifest paths found within the repository
- count of detected package manifests
- signals for npm workspaces, pnpm, Turbo, Nx, and Lerna

This is a useful first step for repository-aware orchestration and package-scoped validation.
